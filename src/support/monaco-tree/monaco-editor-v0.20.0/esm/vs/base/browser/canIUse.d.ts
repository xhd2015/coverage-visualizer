export namespace BrowserFeatures {
    namespace clipboard {
        const writeText: boolean;
        const readText: boolean;
        const richText: boolean;
    }
    const keyboard: number;
    const touch: boolean;
    const pointerEvents: boolean;
}
